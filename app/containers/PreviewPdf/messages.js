/*
 * PreviewPdf Messages
 *
 * This contains all the text for the PreviewPdf container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PreviewPdf';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PreviewPdf container!',
  },
});
