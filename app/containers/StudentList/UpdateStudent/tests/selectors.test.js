// import { selectUpdateStudentDomain } from '../selectors';

describe('selectUpdateStudentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
