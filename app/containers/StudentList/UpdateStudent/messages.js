/*
 * UpdateStudent Messages
 *
 * This contains all the text for the UpdateStudent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UpdateStudent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UpdateStudent container!',
  },
});
