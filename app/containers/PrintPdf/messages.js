/*
 * PrintPdf Messages
 *
 * This contains all the text for the PrintPdf container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PrintPdf';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PrintPdf container!',
  },
});
