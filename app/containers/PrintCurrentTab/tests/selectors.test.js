// import { selectPrintCurrentTabDomain } from '../selectors';

describe('selectPrintCurrentTabDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
