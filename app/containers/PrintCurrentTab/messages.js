/*
 * PrintCurrentTab Messages
 *
 * This contains all the text for the PrintCurrentTab container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PrintCurrentTab';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PrintCurrentTab container!',
  },
});
