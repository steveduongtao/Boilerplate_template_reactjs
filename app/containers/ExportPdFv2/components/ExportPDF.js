import React from 'react';

function ExportPDF() {
  return (
    <div id="demo">
      <div id="block1">
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
        <div style={{ border: '1ppx solid black' }}>
          I tried giving styles using the cell object but it is not reflecting in the downloaded
          .xlsx file. I even tried reading a .xlsx file and writing the same file back using
          XLSX.write() function. But it gives
        </div>
      </div>
    </div>
  );
}

export default ExportPDF;
