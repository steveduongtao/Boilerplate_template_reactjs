/*
 * JsonToExcel Messages
 *
 * This contains all the text for the JsonToExcel container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.JsonToExcel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the JsonToExcel container!',
  },
});
