/*
 * SearchUserGitHub Messages
 *
 * This contains all the text for the SearchUserGitHub container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SearchUserGitHub';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchUserGitHub container!',
  },
});
