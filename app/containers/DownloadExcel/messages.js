/*
 * DownloadExcel Messages
 *
 * This contains all the text for the DownloadExcel container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DownloadExcel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DownloadExcel container!',
  },
});
