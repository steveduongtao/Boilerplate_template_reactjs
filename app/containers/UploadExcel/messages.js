/*
 * UploadExcel Messages
 *
 * This contains all the text for the UploadExcel container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UploadExcel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UploadExcel container!',
  },
});
