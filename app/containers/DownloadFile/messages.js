/*
 * DownloadFile Messages
 *
 * This contains all the text for the DownloadFile container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DownloadFile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DownloadFile container!',
  },
});
