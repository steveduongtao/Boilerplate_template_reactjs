// import { selectDownloadFileDomain } from '../selectors';

describe('selectDownloadFileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
