/*
 * LazyLoadImg Messages
 *
 * This contains all the text for the LazyLoadImg container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LazyLoadImg';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LazyLoadImg container!',
  },
});
